import ResetCodeScreen from '../screens/ResetCodeScreen';

/**
 * Reset Code route component
 * Allows users to enter the 6-digit verification code sent to their email.
 * Upon successful verification, users are redirected to the password reset screen.
 */
export default function ResetCode() {
  return <ResetCodeScreen />;
} 